// External imports, installed libraries, etc

// Internal data, functions, custom hooks, etc
import breakpoints from './breakpoints.js';
import customBreakPoints from './customBreakPoints.js';

// Internal components, images, etc

export default {
	...breakpoints,
	...customBreakPoints,
};